import { Link } from "react-router-dom";

export default function SemesterCard({ semester, onEdit, onComplete, onDelete, busy }) {
  const title = semester.name || `Year ${semester.yearOfStudy} — Semester ${semester.semesterNumber}`;
  const isActive = semester.status === "active";
  return <article className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-bold text-slate-900">{title}</h2><p className="mt-1 text-sm text-slate-600">Academic Year: {semester.academicYear}</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${isActive ? "bg-emerald-100 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{isActive ? "Active" : "Completed"}</span></div><div className="mt-5 flex flex-wrap gap-2"><Link to={`/semesters/${semester._id}/modules`} className="rounded-lg border border-indigo-200 px-3 py-2 text-sm font-medium text-indigo-700">Modules</Link><button onClick={() => onEdit(semester)} disabled={busy} className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-medium disabled:opacity-50">Edit</button>{isActive && <button onClick={() => onComplete(semester)} disabled={busy} className="rounded-lg bg-indigo-600 px-3 py-2 text-sm font-medium text-white disabled:opacity-50">Complete semester</button>}{!isActive && <button onClick={() => onDelete(semester)} disabled={busy} className="rounded-lg border border-red-200 px-3 py-2 text-sm font-medium text-red-700 disabled:opacity-50">Delete</button>}</div></article>;
}
