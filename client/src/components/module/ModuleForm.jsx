import { useState } from "react";
const blank = { moduleCode: "", moduleName: "", credits: "", moduleType: "theory_only", theoryHours: "", practicalHours: "" };
export default function ModuleForm({ module, onSubmit, onCancel, busy }) { const [form, setForm] = useState(() => module ? { ...module } : blank); const [error, setError] = useState(""); const change = (key, value) => { setError(""); setForm((old) => ({ ...old, [key]: value })); }; async function submit(event) { event.preventDefault(); const data = { ...form, credits: Number(form.credits), theoryHours: Number(form.theoryHours), practicalHours: form.moduleType === "theory_only" ? 0 : Number(form.practicalHours) }; if (!data.moduleCode.trim() || !data.moduleName.trim() || data.credits <= 0 || data.theoryHours < 0 || (data.moduleType === "theory_practical" && data.practicalHours < 0)) return setError("Enter valid module details and hour values."); await onSubmit(data, setError); } return <form onSubmit={submit} className="space-y-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="text-xl font-bold">{module ? "Edit module" : "Add module"}</h2><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Module Code<input required maxLength="40" value={form.moduleCode} onChange={(e) => change("moduleCode", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label><label className="text-sm font-medium">Credits<input required min="0.1" step="0.1" type="number" value={form.credits} onChange={(e) => change("credits", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label></div><label className="block text-sm font-medium">Module Name<input required maxLength="160" value={form.moduleName} onChange={(e) => change("moduleName", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label><label className="block text-sm font-medium">Module Type<select value={form.moduleType} onChange={(e) => change("moduleType", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3"><option value="theory_only">Theory Only</option><option value="theory_practical">Theory + Practical</option></select></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Required Theory Hours<input required min="0" step="0.5" type="number" value={form.theoryHours} onChange={(e) => change("theoryHours", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label>{form.moduleType === "theory_practical" && <label className="text-sm font-medium">Required Practical Hours<input required min="0" step="0.5" type="number" value={form.practicalHours} onChange={(e) => change("practicalHours", e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label>}</div>{error && <p className="text-sm text-red-700">{error}</p>}<div className="flex gap-3"><button disabled={busy} className="rounded-lg bg-indigo-600 px-5 py-3 font-semibold text-white disabled:opacity-50">{busy ? "Saving…" : "Save module"}</button><button type="button" onClick={onCancel} className="rounded-lg px-4 py-3">Cancel</button></div></form>; }
