import { Link } from "react-router-dom";
export default function ModuleCard({ module, onEdit, onDelete, busy }) { return <article className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex justify-between gap-4"><div><p className="text-sm font-semibold text-indigo-700">{module.moduleCode}</p><h2 className="mt-1 text-lg font-bold">{module.moduleName}</h2><p className="mt-1 text-sm text-slate-600">{module.credits} credits · {module.moduleType === "theory_only" ? "Theory Only" : "Theory + Practical"}</p><p className="mt-1 text-sm text-slate-500">Theory: {module.theoryHours}h{module.moduleType === "theory_practical" ? ` · Practical: ${module.practicalHours}h` : ""}</p></div></div><div className="mt-5 flex gap-2"><Link to={`/modules/${module._id}`} className="rounded-lg bg-indigo-600 px-3 py-2 text-sm font-medium text-white">View details</Link><button onClick={() => onEdit(module)} disabled={busy} className="rounded-lg border px-3 py-2 text-sm disabled:opacity-50">Edit</button><button onClick={() => onDelete(module)} disabled={busy} className="rounded-lg border border-red-200 px-3 py-2 text-sm text-red-700 disabled:opacity-50">Delete</button></div></article>; }
