function Section({ label, value }) { if (!value) return null; return <div className="rounded-xl bg-slate-50 p-4"><h3 className="font-bold">{label}</h3><dl className="mt-2 grid grid-cols-2 gap-2 text-sm"><dt>Required</dt><dd>{value.requiredHours}h</dd><dt>Conducted</dt><dd>{value.conductedHours}h</dd><dt>Present</dt><dd>{value.presentHours}h</dd><dt>Absent</dt><dd>{value.absentHours}h</dd><dt>Remaining</dt><dd>{value.remainingRequiredHours}h</dd><dt>Attendance</dt><dd className="font-bold">{value.percentage}%</dd></dl></div>; }
export default function AttendanceSummary({ attendance }) { if (!attendance) return null; return <section className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div className="flex flex-wrap justify-between gap-2"><h2 className="text-xl font-bold">Attendance</h2><span className={`rounded-full px-3 py-1 text-sm font-semibold ${attendance.status === "Safe" ? "bg-emerald-100 text-emerald-700" : "bg-amber-100 text-amber-800"}`}>{attendance.status}</span></div><div className="mt-4 grid gap-3 md:grid-cols-3"><Section label="Theory" value={attendance.theory} /><Section label="Practical" value={attendance.practical} /><Section label="Overall" value={attendance.overall} /></div><p className="mt-4 text-sm text-slate-600">Threshold: {attendance.threshold}% · Attendance is calculated from actual conducted hours.</p></section>; }
